# 배포 및 운영 인계서

## 권장 구성

```text
빅카인즈 웹페이지
  └─ embed.js 로드
       ├─ 정적 UI·지식데이터
       └─ /api/chat (선택적 AI 프록시)
              └─ OpenAI Responses API
```

1차 배포는 빅카인즈와 같은 도메인의 `/chatbot/` 경로를 권장합니다. 별도 도메인을 사용하면 CORS와 CSP에 해당 도메인을 등록합니다.

기본 운영 모드는 AI 비활성화 상태이며 정적 FAQ 검색만으로 동작합니다. AI를 사용하더라도 브라우저에 API 키를 넣지 않고 서버의 `/api/chat` 프록시만 사용합니다.

## 환경 구분

- 개발: 샘플·초안 데이터, AI 비활성화 가능
- 검수: 운영 후보 데이터, 접근 제한, 담당자 UAT
- 운영: 승인 데이터만 배포, 서버 비밀키 사용

운영 환경에는 `.env` 파일을 저장소에 커밋하지 말고 배포 플랫폼의 Secret 기능을 사용합니다.

## 배포 체크리스트

- `requiresReview: true`인 정책 문서를 전부 확인
- 관리자 Q&A 엑셀을 가져온 뒤 `work/qna-import-report.json` 검수
- Q&A 데이터에 등록자명·관리자 ID·이메일·전화번호가 없는지 확인
- API 요금표의 금액·부가세·시행일·무료 구간 검수
- 모든 출처 URL이 외부 공개 가능한지 확인
- 문의 링크를 실제 고객지원 URL로 변경
- 허용 출처 `ALLOWED_ORIGIN`을 운영 도메인으로 제한
- OpenAI API 키는 서버에만 설정
- 개인정보·인증키가 로그에 남지 않도록 마스킹 적용
- 접근성 키보드 테스트와 모바일 화면 확인
- 장애 시 AI 없이 기본 검색형 답변이 동작하는지 확인
- 담당자 이관 규칙과 SLA 확인
- 보안 점검과 부하 테스트 수행

## Q&A 갱신 절차

1. 관리자 Q&A를 XLSX로 내려받습니다.
2. 등록자·관리자 ID 같은 불필요한 개인정보 열을 삭제합니다.
3. `node scripts/import-qna.mjs --input "QNA.xlsx"`를 실행합니다.
4. 생성된 `public/data/qna-import.js`와 검수 보고서를 확인합니다.
5. `node scripts/validate-data.mjs`와 회귀 테스트를 실행합니다.
6. `embed.js`의 기본 버전과 웹페이지의 `data-version`을 같은 새 값으로 변경합니다.
7. 검수 환경에서 승인 후 운영에 배포합니다.

질문 본문은 기본적으로 지식베이스에 포함하지 않습니다. 검색 품질 때문에 본문이 꼭 필요할 때만 `--include-question-body` 옵션을 사용하고 개인정보 검수를 추가로 수행합니다.

## 운영 모니터링 권장 지표

- 전체 질문 수와 유형별 비율
- 답변 성공률과 담당자 이관률
- 근거 없음 비율
- 답변 만족·불만족 비율
- API 요금 관련 질문과 이관 사유
- 평균·95백분위 응답시간
- AI 프록시 실패율과 비용
- 자주 등장하는 미등록 질문

질문 원문을 장기 보관하지 않고, 분석에는 유형·문서 ID·신뢰도·만족도처럼 비식별 정보 위주로 사용합니다.

## 장애 대응

- AI API 장애: 프런트엔드가 자동으로 로컬 검색 답변 사용
- 요금정책 검수 미완료: 금액 안내를 중단하고 담당자 이관
- 잘못된 답변 발견: 해당 문서에 `requiresReview: true` 설정 후 수정·검수
- 지식데이터 로드 실패: 챗봇 스크립트 배포 상태와 CSP 확인

## 롤백

정적 파일과 서버 이미지를 버전 태그로 배포합니다. 문제 발생 시 직전 검수 완료 이미지로 되돌리고, 요금정책 데이터는 별도 변경 이력을 유지합니다.

지식데이터 파일은 애플리케이션 코드와 분리해 버전 관리하므로 잘못된 FAQ나 Q&A가 배포된 경우 직전 `official-faq.js`, `verified-policy.js`, `qna-import.js`만 복구할 수 있습니다.

## 최종 연동 전에 필요한 결정

- 운영 인프라: 기존 WAS, 컨테이너 플랫폼, 서버리스 중 선택
- AI 사용 여부와 모델
- 문의 접수 시스템 연동 방식
- 로그·통계 저장소
- 개인정보 영향평가 및 보안 심사 범위

